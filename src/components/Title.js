import React from "react";

function Title(){
    return (
        <div>
            <button type="button" className="btn btn-light btn-block">
                Add Column
            </button>
        </div>
    );
}

export default Title