import React from "react";

function Reusable(){
    return (
        <div>
            <button type="button" className="btn btn-light btn-block">
                Add Column
            </button>
        </div>
    );
}

export default Reusable;